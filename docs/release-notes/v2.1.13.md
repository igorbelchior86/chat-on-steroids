## 2.1.13: Skills and reliability

Skills now support complete Codex-style packages and discovery. Open **+ → Skills** to import a Markdown/text skill or a package directory, or use skills already present in ancestor project `.agents/skills`, the selected project's `.codex/skills`, `~/.agents/skills`, `$CODEX_HOME/skills`, bundled `.system`, and the platform admin location. Type **/** to autocomplete the exact command; `/prompt <command>` remains supported. Duplicate skill names receive deterministic qualified commands instead of silently selecting one source.

Selected skills are inserted below the main prompt and above AGENTS.md and your task. The existing 96,000-character limit keeps explicit skills and user text complete, using less AGENTS.md when space is tight. Codex `config.toml` skill settings can disable individual skills, suppress bundled skills, disable the implicit skill index, or bound its context budget. Standard global aliases reject structured Core file mutations; `/skills` remains the managed install location. Skills do not add tools or run package scripts automatically.

This release includes the **2.1.12 ChatGPT model-picker fix** for main chats and workers, plus local fixes for request attribution, repeated transport errors, answer completion, Goal/Loop recovery, worker revival and clearer tool/plugin failures. Contributor credit remains in `CONTRIBUTORS.md`.

The end of Setup and a one-time reminder on the first model-discovery opening explain ChatGPT's tool approval prompts. Check the ChatGPT window for **Allow once / Always allow / Deny** when Goal, Loop or an agent appears to be waiting. Acknowledging the CoS reminder does not approve a tool action.

Image follow-ups keep their place in the transcript even when preview storage is full, with retained previews and separate delivery receipts. New project chats explicitly name their primary working folder, including projects without AGENTS.md.

Setup now walks through checking Core's Actions list. Health checks distinguish tunnel connectivity, actual catalog responses, and tool requests. Stale calls reaching the wrong connector get specific guidance instead of a misleading generic disabled-plugin message. Native-source packaging retries transient download failures while preserving every source checksum.

**Reload the companion extension and refresh your ChatGPT tabs after updating.** Refresh the CoS connectors when prompted. [Skills guide](https://github.com/totec448-spec/chat-on-steroids/blob/v2.1.13/docs/skills.md).

### Downloads

| Platform | Files |
| --- | --- |
| Windows | `Chat-On-Steroids-Setup-x64.exe` · `Chat-On-Steroids-Setup-arm64.exe` |
| macOS Intel | `Chat-On-Steroids-macOS-x64.dmg` · `Chat-On-Steroids-macOS-x64.zip` |
| macOS Apple silicon | `Chat-On-Steroids-macOS-arm64.dmg` · `Chat-On-Steroids-macOS-arm64.zip` |
| Linux x64 | `Chat-On-Steroids-Linux-x64.AppImage` · `Chat-On-Steroids-Linux-x64.deb` |
| Linux ARM64 | `Chat-On-Steroids-Linux-arm64.AppImage` · `Chat-On-Steroids-Linux-arm64.deb` |

Also included: `Chat-On-Steroids-Extension.zip`, `Chat-On-Steroids-Native-Sources.tar.gz` (corresponding native-library sources and replacement instructions), and `SHA256SUMS.txt`.

macOS builds are publisher-unsigned and unnotarized; macOS 13 Ventura or newer is required. On Linux, prefer the DEB on Debian/Ubuntu; AppImage can fall back to `--no-sandbox` where unprivileged user namespaces are disabled.
